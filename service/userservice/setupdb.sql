CREATE TABLE  usersTraning(
 id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    phone_number VARCHAR(255) NOT NULL UNIQUE,
    created_At TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



